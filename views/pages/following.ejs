<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= userFollowing.firstname %> <%= userFollowing.lastname %></title>
    <%- include("../partials/meta") %>
  </head>
  <body>
    <div class="ms-3 row">
      <!--       NavBar -->
      <div class="col-2 border p-0"><%- include("../partials/navbar") %></div>
      <!--       Body -->
      <div class="col-10 col-lg-7 p-0">
        <div class="px-3 pt-3">
          <!--           Flecha Atrás -->
          <div class="d-flex align-items-center mt-4">
            <a
              class="text-decoration-none text-black fs-4"
              href="/usuarios/<%= userFollowing.username %>"
              >🡠</a
            >
            <div class="d-flex align-items-start flex-column ms-4">
              <h5 class="mb-0">
                <%= userFollowing.firstname %> <%= userFollowing.lastname %>
              </h5>
              <small>@<%= userFollowing.username %></small>
            </div>
          </div>
        </div>
        <div class="mt-3 d-flex justify-content-around">
          <div>
            <a
              href="followers"
              class="fs-5 text-black fw-semibold text-decoration-none"
              >Followers</a
            >
          </div>
          <div>
            <a class="fs-5 text-black fw-semibold text-decoration-none"
              >Following</a
            >
            <div
              style="
                background-color: #1d9bf0;
                width: 100%;
                height: 4px;
                margin-top: 8px;
              "
            ></div>
          </div>
        </div>
        <div class="d-flex flex-column border-top pt-4">
          <!-- TO DO: Hacer que entre a el arreglo following dentro del Modelo User -->
          <% for (const smallUser of users){%>
          <!-- comment -->
          <!-- comment -->
          <%- include("../partials/smallUser", { smallUser, globalUser }) %>
          <!-- comment -->
          <%}%>
        </div>
      </div>
      <!--       More info -->
      <div class="col-4 d-none d-lg-block col-lg-3 border p-0">
        <%- include("../partials/moreinfo", { usersInfo, globalUser }) %>
      </div>
    </div>
  </body>
</html>
